# Upstream Issues

Flutter SDK·Canvas·Skia 렌더러 등 **pixel_ui 자체 수정으로 해결 불가**한 이슈를 모아 격리 추적하는 폴더.

`/dogfood run` 중 `upstream` 분류된 이슈는 GitHub Issue가 아니라 이 폴더의 개별 `.md` 파일로 기록된다. 템플릿은 `.claude/skills/dogfood/upstream-template.md` 참조.

## 수집됨

(없음)

## 추적 중

(없음)

## Flutter 이슈 연결됨

(없음)

## 해결됨

(없음)

---

항목이 생기면 상태에 따라 아래 형식으로 한 줄 링크:
`- [{제목}]({slug}.md) — {한줄 요약}`
